[project]
name = "financial-rag"
version = "0.1.0"
dependencies = [
    "fastapi", "uvicorn", "pydantic", "llama-index", "weaviate-client",
    "sentence-transformers", "langdetect", "cohere", "python-dotenv"
]
