# Financial Statements RAG System

Retrieval-Augmented Generation (RAG) for ingestion, extraction and analysis of financial statements and reports (English + Hebrew).

Quickstart
1. Clone the repo and create a virtualenv
2. pip install -r requirements.txt
3. Edit config/config.yaml
4. Ingest documents (CLI/GUI) and run queries

See docs/ for usage and architecture.